library(tabshiftr)
library(testthat)
library(checkmate)
context("nested")


test_that("identifying variable that is nested into another identifying variable", {

  # input <- tabs2shift$nested_variable
  #
  # schema <- setIDVar(name = "territories", columns = 1) %>%
  #   setIDVar(name = "sublevel", columns = 2, nested = "territories") %>%
  #   setIDVar(name = "year", columns = 7) %>%
  #   setIDVar(name = "commodities", columns = 2) %>%
  #   setObsVar(name = "harvested", columns = 5) %>%
  #   setObsVar(name = "production", columns = 6)
  #
  # .expect_valid_table(x = reorganise(input = input, schema = schema), units = 3, groups = TRUE)

})
